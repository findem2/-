// 상수 num을 2로 초기화 한다
const num = 2;

// 상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화한다.
const num = 2; 

//상수 num 을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 Num을 2로 초기화한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을  2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

// 싱스 num을 2로 초기화 한다
const num = 2;

// 상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다 .
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 Num을 2로 초기화 한다
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 Num을 2로 초기화 한다.
const num = 2;

// 상수 Num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 힌디.
const num = 2;

//상수 nun을 2로 초기화 한다.
const num = 2;

// 상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num =  2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

// 상수 num을 2로 초기화 한다.
const num = 2; 

//상수 num을 2로 초기화 한다.
const num = 2;

// 상수 num을 2로 초기화한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

// 상수 num을 2로 초기화 한다
const num = 2;

// 상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화한다.
const num = 2; 

//상수 num 을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 Num을 2로 초기화한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;

//상수 num을  2로 초기화 한다.
const num = 2;

//상수 num을 2로 초기화 한다.
const num = 2;
