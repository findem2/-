// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr1에 1과 2와 3을 가지고 있는 배열을 초기화 한다.
let arr1 = [1, 2, 3];

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();

// 변수 arr2에 새배열 인스턴스를 초기화 한다.
let arr2 = new Array();