// 만약에 라면
if () {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
  num = num / 2;
  // num /= 2;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
  B;
} else {
  C;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
  b;
} else if (c) {
  d;
} else {
  e;
}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 라면
if () {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 Num이 10보다 작다면 
if (num < 10) {

}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}
 
// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 num이 10보다 크다면 num을 2로 나누어서 재할당한다.
// 만약에 num이 10보다 크다면 2로 나눈다.
if (num > 10) {
    num = num / 2;
    // num /= 2;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 A라면 B 아니면 C해라
if (A) {
    B;
} else {
    C;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}

// 만약에 a라면 b하고 c라면 d하고 아니면 e해라
if (a) {
    b;
} else if (c) {
    d;
} else {
    e;
}
